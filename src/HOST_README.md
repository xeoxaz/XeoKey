# XeoKey Host Wrapper (Deprecated)

The host wrapper is deprecated and no longer used in the recommended deployment flow.

Use one of these instead:

- `bun run start` for direct runtime
- systemd service management for production restart and boot supervision

For systemd setup, see `../SYSTEMD_SETUP.md`.
